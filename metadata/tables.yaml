- table:
    schema: public
    name: todos
  object_relationships:
  - name: user
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: users
        column_mapping:
          user_id: id
- table:
    schema: public
    name: users
  array_relationships:
  - name: todos
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: todos
        column_mapping:
          id: user_id
